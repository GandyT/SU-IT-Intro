/* nodejs server does not have fetch. Must be installed as a library */
const fetch = require("node-fetch")

const getUrl = "localhost:8000/api/getdata"
const postUrl = "localhost:8000/api/updatedata"

const run = async () => {
    
}